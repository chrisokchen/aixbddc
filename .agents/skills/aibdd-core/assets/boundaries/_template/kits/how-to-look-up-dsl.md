# 設定
1. <FILL-IN：cli 指令中可能用到的參數設定？>

# 指令

請參考底下指令來善用 dsl_cli query 來找到該 step format 的候選 DSL。

```bash
    uv run .claude/skills/aibdd-core/scripts/run_dsl_cli.py query \
    <FILL-IN：這邊填寫 cli 的參數>
```

